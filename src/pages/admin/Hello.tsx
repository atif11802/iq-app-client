import React from "react";
import Sidebar from "../../components/Sidebar";

type Props = {};

const Hello = (props: Props) => {
	return <div>Hello</div>;
};

export default Hello;

Hello.Layout = Sidebar;
